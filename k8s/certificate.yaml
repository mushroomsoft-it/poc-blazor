apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: blazor-cert
  namespace: poc-blazor
spec:
  secretName: blazor-tls
  issuerRef:
    name: letsencrypt-godaddy
    kind: ClusterIssuer
  dnsNames:
    - dev.n8nmushroomsoft-it.com
